rules_version = '2';
// รูปที่อัปโหลดเข้าถึงผ่าน backend (Admin SDK) เท่านั้น ปิด client ตรงทั้งหมด
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
